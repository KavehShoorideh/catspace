> **A repository restructure occurred on 2026-08-03.** File and directory paths
> mentioned below are historical and no longer valid. The content is left exactly
> as written — this is the research record, not documentation of the current tree.
> For the current layout see [`repo_structure.md`](../../../../repo_structure.md); for what
> moved and why see [the refactor plan](../../../../catspace/research/docs/2026-08-03-refactor-plan.md).

# archive/ — superseded design docs and dated results

Nothing here is current. These are kept because they are the record of how the design got
where it is: the reasoning that was live at the time, the results as they were reported,
and the plans that were later abandoned or replaced.

The banner above applies to every file in this directory. None of them were rewritten
during the 2026-08-03 restructure, so their paths describe the pre-restructure tree.

For what is current: `catspace/research/docs/` one level up, and `repo_structure.md` at the
repo root.
